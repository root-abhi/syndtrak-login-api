apiVersion: v1
kind: Service
metadata:
  name: login-api
  namespace: backend
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "classic"
spec:
  type: LoadBalancer
  selector:
    app: login-api
  ports:
    - port: 80
      targetPort: 3000
